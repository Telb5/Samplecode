package Utils;

import java.security.PublicKey;

public class Constants {

    public static final int WAIT_TIME = 1000;

    public static final String PROPERTY_FILE_PATH = System.getProperty("user.dir")+"/src/test/resources/Config/config.properties";
    //when using "user.dir", we might need to change \ to / in our path if we have them.
    //usually Windows users have this issue.

    public static final String SCREENSHOT_FILEPATH = System.getProperty("user.dir") +"/screenshots/";


// Constatnt variables are written in caps
}
